import 'package:flutter/material.dart';

Widget appbar(BuildContext context, String tit) {
  return AppBar(
    backgroundColor: Colors.grey.shade900,
    title: Text(
      tit,
    ),

    //Other data you want to show
  );
}
